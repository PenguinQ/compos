import { Canvas, Meta, ArgTypes } from '@storybook/blocks';

import * as QuantityEditorStories from './QuantityEditor.stories';
import QuantityEditor from './QuantityEditor.vue';

<Meta title="Components/QuantityEditor" of={QuantityEditorStories} />

# QuantityEditor
QuantityEditor is a component to + or - things, argh... idk the word. 🗿

## Usage
To start using **QuantityEditor**, see the example below:

<Canvas of={QuantityEditorStories.DocUsage} />

## Props
<ArgTypes of={QuantityEditor} />

## Examples
### Non Two-Way Data Binding
You can also use **QuantityEditor** without using v-model two-way data binding using:

1. @change
2. @clickIncrement
3. @clickDecrement

For the sake of simplicity, we're using option **2 and 3**, see the example below:

<Canvas of={QuantityEditorStories.DocNonTWDB} />
